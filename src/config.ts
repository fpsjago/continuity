/**
 * Template config — change these for your business.
 * WHATSAPP: full international number, digits only (no +, spaces or dashes).
 */
export const WHATSAPP = '50766259817'; // ← placeholder demo number, replace
export const BRAND = 'CONTINUITY';
